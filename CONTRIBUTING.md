# コントリビューションガイド (Contributing Guidelines)

monopo (モノポ) プロジェクトへのご関心、ありがとうございます！
皆様からのコントリビューション（バグ報告、機能追加、ドキュメント改善など）を歓迎します。

以下のガイドラインに沿ってプロジェクトにご協力ください。

## 開発環境のセットアップ

本プロジェクトはパッケージマネージャーとして **必ず `bun` を使用してください**。`npm` / `yarn` / `pnpm` は使用しないでください。また、Bun は v1.2 以上、Node.js は v22 (LTS) 以上を推奨します。なお、`bun audit` は現在 `bun.lock` テキスト形式に十分対応していないため、CVE スキャンは当面 Dependabot に委ねています。

```bash
# 依存関係のインストール
bun install

# 開発サーバーの起動
bun run dev
```

## コーディング規約とプロジェクトルール

コードの品質と一貫性を保つため、以下のルールに従ってください：

1. **名称に関する注意 (重要)**
   - 商標上の理由により、UI、ドキュメント、ファイル名、ローカルストレージのキーなど、リポジトリ全体で必ず「**monopo**」または「**モノポ**」という名称を使用してください。（「monopoly」や「モノポリ」は使用厳禁です）
2. **コンポーネントの使用**
   - インタラクティブな要素（ボタンなど）には、生HTMLの `<button>` ではなく、必ず共通コンポーネントである `<Button>` (`src/components/common/Button.tsx`) を使用してください。これによりスタイルとアクセシビリティが一貫します。
3. **アクセシビリティ**
   - 新しいコンポーネントやインタラクティブ要素を追加する際は、キーボード操作の対応や適切な `aria-label` の設定など、アクセシビリティに配慮してください。
4. **スタイリング**
   - スタイリングには CSS Modules を使用してください。グローバルなCSSの追加は避け、各コンポーネントごとのモジュールファイルで定義してください。

## 変更の検証 (テストとリント)

プルリクエストを作成する前に、変更が既存の機能を壊していないこと、またコーディング規約に沿っていることを確認してください。

```bash
# コードのフォーマット
bun run format

# 静的解析 (Lint)
bun run lint

# TypeScriptの型チェック
bun run typecheck

# テストの実行
bun run test
```

すべてのチェックとテストが通過することを確認してからコミットを行ってください。

## プルリクエスト (PR) の作成手順

1. リポジトリをフォークし、ローカルにクローンします。
2. 作業用の新しいブランチを作成します。（例: `feature/add-new-board-space`, `fix/dice-roll-bug`）
   ```bash
   git checkout -b feature/your-feature-name
   ```
3. 変更を加え、コミットします。コミットメッセージはわかりやすく簡潔に記述してください。
4. フォークしたリポジトリにプッシュします。
   ```bash
   git push origin feature/your-feature-name
   ```
5. 本リポジトリに対してプルリクエストを作成します。
6. PRを作成する際は、テンプレート（`.github/PULL_REQUEST_TEMPLATE.md`）に沿って必要な情報を記入してください。

## セキュリティに関する報告

セキュリティ上の脆弱性を発見した場合は、公開のIssue等で報告せず、リポジトリの管理者宛に直接ご報告いただくようお願いいたします。詳細は `SECURITY.md` を参照してください。

### ローカル環境での漏洩防止 (Secret Leak Prevention)

本プロジェクトでは、API キーや環境変数などの秘密情報が誤って公開リポジトリへコミットされるのを防ぐため、以下の対策を設けています。詳細は `SECURITY.md` をご確認ください。

- **gitleaks の必須化**: コミット時に pre-commit フックでローカルスキャンを行います。事前に `gitleaks` をインストールしてください。
- **エディタの安全設定**: `.vscode/settings.json` により、`.env` や鍵ファイル、AI エージェントの作業ディレクトリ（`.cursor/`, `.claude/` など）を検索・ファイルツリーから除外し、画面共有時や AI による自動読み込みによる漏洩を防いでいます。
- **.gitattributes 保護**: 秘密情報ファイルの差分表示をブロック (`-diff`) しています。

---

ご不明な点があれば、Issueでお気軽にご質問ください！
